1，配置修改
修改src/configurable_info.h中的内容
（1）修改线上环境
CLOUD_TYPE：0代表测试环境，1代表uat环境，2代表生产环境
（2）修改appkey和secretkey
MY_APPKEY/MY_SECKEY：根基项目实际使用的appkey和secretkey填写
（3）修改工控机http server的ip和端口（终端调用）
MY_SERVER_URL：工控机url
（4）云端接口url
CLOUD_AUTH_URL：ig云认证url
MC_REGISTER_URL_CP：连接平台注册url
CLOUD_UC_TS_URL：时间戳获取url

2，编译
./build.sh

3，云端配置
提供mac地址，即udid给到连接平台的同事进行空间和授权配置

4，启动
已安装了supervisor，默认开机启动，可以通过如下命令查看：
ps aux | grep http_server
手动启动的方法为：
（1）关闭supervisor
sudo systemctl stop supervisord.service
（2）执行
./run.sh
